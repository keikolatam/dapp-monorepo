/// Keiko shared UI package — Atomic Design + Material 3.
///
/// Barrel export. Components live under `lib/` organized as
/// atoms → molecules → organisms (see `/atomic-design-toolkit:generate`).
library keiko_ui;

export 'theme/index.dart';
