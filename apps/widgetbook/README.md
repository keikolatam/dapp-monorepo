# apps/widgetbook

Catálogo de componentes Flutter (Widgetbook) para `packages/keiko_ui` — atoms → molecules → organisms sobre Material 3. Convención compartida con vertivo/altrupets/aduanext (`apps/widgetbook`).

> Scaffolding inicial (Fase 0). Se puebla junto con `packages/keiko_ui` en el issue A (UI del coach).
