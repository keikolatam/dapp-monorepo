{
  "name": "keiko",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "shared/*"
  ],
  "scripts": {
    "build": "cargo build --release",
    "test": "cargo test",
    "lint": "cargo clippy",
    "dev": "cargo leptos watch",
    "serve": "cargo leptos serve"
  }
}
